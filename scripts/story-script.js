/*$(document).ready(function(){
    
 var field = $.trim($("#myForm input").val());
    
    if(field != ""){
            
        $.ajax({
            type: "post",
            url: "story-index.php",
            data: "data",
            }); 
        
        alert ("hiiii");
            
        } else {
              $('#myForm').on('submit',function (e) {
                e.preventDefault();   
            })
        }*/

 
/*
    $('#myForm input').blur(function() {
     if(!$.trim(this.value).length) { // zero-length string AFTER a trim
                $('#myForm').on('submit',function (e) {
                e.preventDefault();
        });
     }
});
*/

});    
